// Deprecated location — the typography tokens now live in constants/theme/.
// Kept as a re-export for existing `@/constants/typography` imports.
export { typography, font, type Typography, type DisplayWeight, type BodyWeight } from './theme/typography';
